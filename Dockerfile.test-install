FROM python:3.9-slim

WORKDIR /app

# Install essential build and test dependencies
RUN apt-get update && apt-get install -y \
    build-essential \
    wget \
    unzip \
    git \
    systemd \
    dbus \
    python3-dev \
    sudo \
    && rm -rf /var/lib/apt/lists/*

# Copy all files
COPY . .

# Set execute permissions
RUN chmod +x install/install.sh \
    && chmod +x scripts/*.sh 2>/dev/null || true \
    && chmod +x manage_radio.sh 2>/dev/null || true

# Set environment variables for testing
ENV MOCK_SERVICES=true \
    SKIP_HARDWARE=1 \
    SKIP_NETWORK_SETUP=1 \
    SKIP_PIGPIO=1 \
    DEV_MODE=true \
    TEST_MODE=1 \
    DOCKER_ENV=1

# Run install script with test flags and skip systemd operations
RUN SKIP_PIGPIO=1 TEST_MODE=1 DOCKER_ENV=1 ./install/install.sh --test || exit 0

# Skip systemd in test environment
CMD ["bash", "-c", "echo 'Test environment ready' && sleep infinity"]
